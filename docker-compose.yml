version: '3.8'

services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "5001:5000"
    volumes:
      - ./server:/app
      - /app/node_modules
    environment:
      - MONGO_URI=mongodb+srv://golipavansaikrishna2608_db_user:Jc1ar3ufR6gjaqxq@cluster0.bvyloaz.mongodb.net/?appName=Cluster0
      - JWT_SECRET=indiamsme2024secretkey
      - FLASK_URL=http://ai-service:8000
      - PORT=5000
    depends_on:
      - ai-service

  ai-service:
    build: ./ai-service
    ports:
      - "8000:8000"
    volumes:
      - ./ai-service:/app
